model User {
  id       Int    @id @default(autoincrement())
  email    String @unique
  password String
}

model Saved {
  id       Int    @id @default(autoincrement())
  email    String
  name     String
  location String
  fees     Int
  rating   Float
}